{
  "version": 2,
  "buildCommand": "cp -r public/* client/public/ 2>/dev/null; cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" }
  ]
}
